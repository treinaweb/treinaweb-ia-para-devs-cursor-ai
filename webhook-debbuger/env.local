# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# Webhook Configuration
# TTL in seconds (default: 86400 = 24 hours)
WEBHOOK_TTL_SECONDS=86400

# Application URL (used for generating webhook URLs)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

